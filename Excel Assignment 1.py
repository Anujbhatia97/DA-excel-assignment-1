#!/usr/bin/env python
# coding: utf-8

# In[ ]:


get_ipython().set_next_input('Q1 What do you mean by cells in an excel sheet');get_ipython().run_line_magic('pinfo', 'sheet')
Ans. A cell is a rectangular area formed by the intersection of a column and a row. Cells are identified by the Cell Name (or Reference, which is found by combining the Column Letter with the Row Number


# In[ ]:


get_ipython().set_next_input('Q2. How can you restrict someone from copying a cell from your worksheet');get_ipython().run_line_magic('pinfo', 'worksheet')
Ans In order to protect your worksheet from getting copied, you need to go into Menu bar >Review > Protect sheet > Password. By entering password, you can secure your worksheet from getting copied by others.


# In[ ]:


get_ipython().set_next_input('Q3. How to move or copy the worksheet into another workbook');get_ipython().run_line_magic('pinfo', 'workbook')
Ans 1. Copy Excel sheet by dragging
    2. Duplicate a sheet by right-clicking
    3. Copy a tab in Excel using the ribbon


# In[1]:


get_ipython().set_next_input('Q4.Which key is used as a shortcut for opening a new window document');get_ipython().run_line_magic('pinfo', 'document')
Ans Ctrl + N


# In[ ]:


get_ipython().set_next_input('Q5 What are the things that we can notice after opening the Excel interface');get_ipython().run_line_magic('pinfo', 'interface')
Ans The main features of MS Excel include inserting a pivot table, sorting of tabulated data, adding formulas to the sheet, and calculating large data.


# In[ ]:


get_ipython().set_next_input('Q6 When to use a relative cell reference in excel');get_ipython().run_line_magic('pinfo', 'excel')
Ans Relative references are used when we want to perform a similar operation on multiple cells and the formula must change according to the relative address of column and row. For example, We want to add the marks of two subjects entered in column A and column B and display the result in column C

